import React from 'react';
import './Header.css';

const Header = (props) => {
    return (
        <div className="Header sticky-top">
           Admin
        </div>
    );
};

export default Header;
